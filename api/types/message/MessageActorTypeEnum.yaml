resource-type: type-definition

name: MessageActorTypeEnum
description: Message Actor Type
type: String
size: 10

enum:
  - endUser = End User
  - agent = Agent
  - bot = Bot
  - supervisor = Supervisor
  - system = System

example: "agent"
