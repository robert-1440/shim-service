resource-type: type-definition

name: MessageEntryTypeEnum
description: Message Entry Type
type: String
size: 15

enum:
  - text = Text
  - media = Media
  - carousel = Carousel
  - endUserOptedIn = End user opted in

example: "text"
